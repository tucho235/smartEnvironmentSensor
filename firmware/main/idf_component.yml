dependencies:
  espressif/network_provisioning:
    version: "^1.1.0"
  espressif/mqtt:
    version: "^1.0.0"
    rules:
      - if: "$CONFIG{APP_ENABLE_MQTT_TELEMETRY} == True"
  espressif/esp_matter:
    version: "^1.6.0"
    rules:
      - if: "$CONFIG{APP_ENABLE_MATTER} == True"
